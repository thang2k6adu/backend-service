apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pp191225-api-service.fullname" . }}-test-connection"
  labels:
    {{- include "pp191225-api-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pp191225-api-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
