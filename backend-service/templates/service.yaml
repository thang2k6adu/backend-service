apiVersion: v1
kind: Service
metadata:
  name: {{ include "pp191225-api-service.fullname" . }}
  labels:
    {{- include "pp191225-api-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pp191225-api-service.selectorLabels" . | nindent 4 }}
